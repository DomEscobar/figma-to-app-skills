{
  "name": "figma-browser-capture-scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Authorized Figma browser capture with stability and provenance checks.",
  "engines": { "node": ">=20.6.0" },
  "scripts": {
    "test": "node --test tests/*.smoke.mjs",
    "postinstall": "echo \"Now run: npx playwright install chromium\""
  },
  "dependencies": {
    "pixelmatch": "^7.2.0",
    "playwright": "^1.62.1",
    "pngjs": "^7.0.0"
  }
}
