{
  "name": "visual-fidelity-loop-scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "CLI scripts for the visual-fidelity-loop Agent Skill: deterministic screenshot capture, pixel diffing, and structured CSS-property checks.",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "postinstall": "echo \"Now run: npx playwright install chromium\""
  },
  "dependencies": {
    "pixelmatch": "^7.2.0",
    "playwright": "^1.62.1",
    "pngjs": "^7.0.0"
  }
}
